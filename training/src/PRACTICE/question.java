package PRACTICE;

public class question {
    public static void main(String[] args) {

        // // 1. given list of array , return a square of only even number
        // List<Integer> numbers = Arrays.asList(1, 2, 3, 4, 5);
        // List<Integer> even = numbers.stream().filter((n)-> n % 2 == 0).map(n ->
        // n*n).collect(Collectors.toList());
        // System.out.println("Even Numbers: "+even);

        // // 2.given a list of names , return a list of number in uppernames which have
        // more than 3 character
        // List<String> names = Arrays.asList("Ram" ,"Shyam","Om" ,"Sita");
        // List<String> uppernames = names.stream()
        // .filter(n -> (n.length()>3))
        // .map(n -> n.toUpperCase())
        // .collect(Collectors.toList());
        // System.out.println(uppernames);

        // 3.find the sum od all odd numbers in a list
        // List<Integer> number = Arrays.asList(1, 2, 3, 4, 5);
        // int sum = number.stream()
        //         .filter(n -> n % 2 != 0)
        //         .reduce(0 , (a,b)-> a + b);
        //         System.out.println("the sum of all odd numbers "+sum);

        // // 4./converts  list of words to their lengths 
        //   List<String> names = Arrays.asList("Ram" ,"Shyam","Om" ,"Sita");
        //   List<Integer> lengthname = names.stream()
        //   .map(n -> n.length())
        //   .collect(Collectors.toList());
        //   System.out.println(lengthname);

        // 5. 
    }
}
